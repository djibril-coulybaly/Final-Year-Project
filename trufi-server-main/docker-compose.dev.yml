version: '3'
services:
  nginx:
    image: nginx:1.15-alpine
    volumes:
      - ./dev/:/etc/nginx/conf.d
    ports:
      - 8080:80
    restart: unless-stopped
      
networks:
  default:
    name: trufi-server
