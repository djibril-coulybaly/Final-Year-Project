import 'package:flutter/material.dart';

class SearchByRoute extends StatelessWidget {
  const SearchByRoute({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}