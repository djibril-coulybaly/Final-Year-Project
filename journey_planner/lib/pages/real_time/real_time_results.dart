import 'package:flutter/material.dart';

class RealTimeResults extends StatelessWidget {
  const RealTimeResults({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}