import 'package:flutter/material.dart';

class SearchByStopNumber extends StatelessWidget {
  const SearchByStopNumber({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}