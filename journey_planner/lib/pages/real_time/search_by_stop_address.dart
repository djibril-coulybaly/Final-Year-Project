import 'package:flutter/material.dart';

class SearchByStopAddress extends StatelessWidget {
  const SearchByStopAddress({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}