import 'package:flutter/material.dart';

class SplashScreenWalkthrough extends StatelessWidget {
  const SplashScreenWalkthrough({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}