import 'package:flutter/material.dart';

class Capping extends StatelessWidget {
  const Capping({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}