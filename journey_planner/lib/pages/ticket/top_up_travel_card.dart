import 'package:flutter/material.dart';

class TopUpTravelCard extends StatelessWidget {
  const TopUpTravelCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}