import 'package:flutter/material.dart';

class PlanRouteResults extends StatelessWidget {
  const PlanRouteResults({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}